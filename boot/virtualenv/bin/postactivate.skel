#!/bin/bash
# This hook is run after this virtualenv is activated.

export PROJECTROOT="{{path}}"
export IPYTHONDIR=$PROJECTROOT/conf/ipython

cd $PROJECTROOT
PS1="\[\e[34;1m\](`basename \"$VIRTUAL_ENV\"`)\[\e[0m\] $_OLD_VIRTUAL_PS1"

